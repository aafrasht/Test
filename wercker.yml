box: golang

dev:
    steps:
        - setup-go-workspace:
            package-dir: github.com/aafrasht/Test

        - script:
          name: env
          code: env

        - internal/watch:
          code: go run wercker.go
          reload: true

build:
    steps:
        - setup-go-workspace:
            package-dir: github.com/aafrasht/Test

        - script:
            name: env
            code: env

        - script:
            name: go build
            code: |
              go build

